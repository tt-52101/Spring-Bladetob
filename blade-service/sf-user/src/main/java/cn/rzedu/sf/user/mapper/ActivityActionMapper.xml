<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rzedu.sf.user.mapper.ActivityActionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="activityActionResultMap" type="cn.rzedu.sf.user.entity.ActivityAction">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="name" property="name"/>
        <result column="send_count" property="sendCount"/>
        <result column="click_count" property="clickCount"/>
        <result column="create_date" property="createDate"/>
        <result column="modify_date" property="modifyDate"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>


    <select id="selectActivityActionPage" resultMap="activityActionResultMap">
        select * from sf_activity_action where is_deleted = 0
    </select>

    <update id="updateSendCount">
        UPDATE sf_activity_action
        SET send_count = send_count + #{number}
        WHERE `type` = #{type}
    </update>

    <update id="updateClickCount">
        UPDATE sf_activity_action
        SET click_count = click_count + #{number}
        WHERE `type` = #{type}
    </update>
</mapper>
